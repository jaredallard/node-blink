{
  "name": "blink-darksky",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "BSD",
  "description": "use a blink(1) device with darksky to give ambient weather predictions",
  "dependencies": {
    "darksky": "~0.1.2",
    "node-blink": "0.0.2"
  }
}
